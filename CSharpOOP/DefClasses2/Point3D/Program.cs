namespace Point3D
{
    using System;
    using System.Collections.Generic;

    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Point3D.StartPoint);

            //Path newPath = new Path(new Point3D(), new Point3D(1,0,4), new Point3D(-5,6,5));
            //PathStorage.SavePathsInFile(newPath);
            //Path secondPath = new Path();
            //secondPath = PathStorage.LoadPathsFromFile(@"../../PathStorage.txt");
            //Console.WriteLine(secondPath);

        }
    }
}
